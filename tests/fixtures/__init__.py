"""Recorded provider responses used by the web-research tests.

Every fixture here is real output captured from a live provider, trimmed to a
few results. Tests parse these rather than calling the network, so the suite
stays offline, deterministic, and spends none of the real search budget.
"""
